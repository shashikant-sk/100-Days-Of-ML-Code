## 2024-05-18 - Adding Alt Text to Markdown Images
**Learning:** HTML `<img>` tags used in Markdown files for infographics and charts require descriptive `alt` text to ensure accessibility for screen readers.
**Action:** Verify and add missing `alt` attributes to HTML image elements in Markdown files.
