## 2024-10-24 - Optimizing Documentation Performance
**Learning:** Repositories consisting mostly of Markdown documentation can still be optimized for web performance. When Markdown uses HTML tags like `<img>` instead of standard Markdown syntax, standard web optimization techniques apply.
**Action:** Always check for embedded HTML within Markdown documentation, and apply standard HTML optimizations like `loading="lazy"` to images.
